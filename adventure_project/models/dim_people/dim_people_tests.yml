version: 2

models:
  - name: dim_people
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: type
        tests:
          - accepted_values:
              values: ['EM', 'GC', 'IN', 'SC', 'SP', 'VC']
